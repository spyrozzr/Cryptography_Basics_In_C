#include <stdio.h>
#include <stdlib.h>

int main(){
  //Read string, read unsigned int, encrypt string based on int and return the result.
  //Also make function that prints all possible ciphers to decrypt the string.
}
